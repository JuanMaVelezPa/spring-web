spring.datasource.url=${DB_URL:jdbc:postgresql://postgres:5432/companydb}
spring.jpa.hibernate.ddl-auto=update
spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}

# Behind Nginx (docker-compose `web` → `app`): correct scheme/host when building absolute URLs.
server.forward-headers-strategy=framework
